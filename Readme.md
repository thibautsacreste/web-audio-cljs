# Web Audio in Clojurescript

# Development

Starting the dev web server:
```
boot dev
```

Connect to the browser repl:
```
boot repl -c
...
boot.user=> (start-repl)
```

Visit http://localhost:3000
